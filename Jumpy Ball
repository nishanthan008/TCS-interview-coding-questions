#include <iostream>
#include <unordered_map>
using namespace std;
int main()
{
	    int n,distance=0;
	    cout<<"enter your value :";
	    cin>>n;
	    distance=2*n;
	    while(n!=0)
	    {
	        distance+=2*(n/2);
	        n=n/2;
	    }
	    cout<<"total distance:"<<distance;
}
