#include <iostream>
using namespace std;
int main()
{
	    string x,S;
	    cout<<"enter your string:";
	    cin>>S;
	    for(int i=0;i<S.size();i++)
	    {
	        if(S[i]!='a'&&S[i]!='e'&&S[i]!='i'&&S[i]!='o'&&S[i]!='u')
	        {
	           x+=S[i];
	        }
	    }
	    cout<<x;   
}
